<html>
<head>
  <style>
    table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }

    td, th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
        font-size: 16px;
        background-color: rgba(255,255,255,0.5);
        color: black;
    }

    tr:nth-child(even) {
        background-color: #dddddd;
    }

    h1 {
        color: DeepSkyBlue;
        /* background-color: rgba(255,255,255,0.5); */
    }

    h2 {
        color: DeepSkyBlue;
        /* background-color: rgba(255,255,255,0.5); */
    }

  </style>
</head>

<body background="http://bedfordandbowery.com/wp-content/uploads/2017/06/FullDelivery_BedfordBowery_GovernorsBall2017_DanielLeinweber-RazberryPhotography_06-copy.jpg">

<table><h2>Performance Stages</h2>

<tr>
  <th>Stage</th>
  <th>Artist</th>
  <th>Start Time</th>
  <th>End Time</th>
  <th>Add to My Schedule</th>
</tr>
  <% @stages.each do |stage| %>
  <tr>
    <% stage.sorted_performances.each do |performance| %>
      <td> <%= stage.name %> </td>
      <td> <%= performance.artist.name %> </td>
      <td> <%= performance.start_time.strftime("%A, %x at %l:%m%p") %> </td>
      <td> <%= performance.end_time.strftime("%A, %x at %l:%m%p") %> </td>
      <td> <%= button_to "Add to My Schedule", bookings_path(:performance_id => performance, :schedule_id => @user.schedule)%> </td>
    </tr>
    <% end %>
  <% end %>
</table>
<%#= render "common/nav_all" %>

</body>

</html>
